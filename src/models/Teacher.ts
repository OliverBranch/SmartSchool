export class Teacher{
    public id: Number; 
    name: string;
    subject: string;

    constructor (id:Number, name:string, subject:string){
        this.id = id;
        this.name = name;
        this.subject = subject;
    }

}